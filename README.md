# Melbourne Property Price Predictor

Predicts residential property sale prices using the Melbourne Housing dataset.

## Tools used
Python, XGBoost, scikit-learn, SHAP, pandas, seaborn

## Results
- Best model: XGBoost (R² = 0.XX, RMSE = $XXX,000)
- Key drivers: Distance from CBD, Building Area, Rooms

## How to run
1. Download `melb_data.csv` from Kaggle
2. Install dependencies: `pip install -r requirements.txt`
3. Open `melbourne_property_predictor.ipynb` in Jupyter

Step 5 — Share the link
Your repo will be live at github.com/yourusername/melbourne-property-predictor — put this link directly on your resume.

That's it! No command line needed. Once you're comfortable with this flow, it's worth learning Git commands (git add, git commit, git push) as they're expected knowledge for most data science roles.Sonnet 4.6
